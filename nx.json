{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "tasksRunnerOptions": {
    "default": {
      "runner": "nx/tasks-runners/default",
      "options": {
        "cacheableOperations": ["build", "lint", "test", "e2e"]
      }
    }
  },
  "workspaceLayout": {
    "libsDir": "gems"
  },
  "namedInputs": {
    "sharedGlobals": [
      "{workspaceRoot}/Gemfile",
      "{workspaceRoot}/Gemfile.lock"
    ],
    "default": ["{projectRoot}/**/*", "sharedGlobals"],
    "production": ["default", "!{projectRoot}/spec/**/*"]
  },
  "targetDefaults": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["BUNDLE_GEMFILE=$PWD/../../Gemfile bundle exec rake build"]
      },
      "inputs": ["production", "^production", { "runtime": "ruby -v" }],
      "outputs": ["{projectRoot}/pkg"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "BUNDLE_GEMFILE=$PWD/../../Gemfile bundle exec standardrb -P -f simple -o .rubocop_status"
      },
      "inputs": [
        "default",
        "{workspaceRoot}/.standard.yml",
        { "runtime": "ruby -v" }
      ],
      "outputs": ["{projectRoot}/.rubocop_status"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["BUNDLE_GEMFILE=$PWD/../../Gemfile bundle exec rake spec"]
      },
      "inputs": ["default", "^production", { "runtime": "ruby -v" }],
      "outputs": ["{projectRoot}/.rspec_status"]
    }
  }
}
